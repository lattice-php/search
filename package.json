{
  "name": "@lattice-php/search",
  "version": "0.53.1",
  "private": true,
  "description": "Dev workspace for the search component package; ships to consumers via Composer, not npm.",
  "type": "module",
  "scripts": {
    "build:standalone": "vite build",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@lattice-php/core": "0.53.1",
    "@lattice-php/ui": "0.53.1"
  }
}
